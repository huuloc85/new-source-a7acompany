{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "format": "npx prettier --write resources/",
        "lint": "npx prettier --check resources/",
        "prepare": "husky"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "husky": "^9.1.7",
        "laravel-vite-plugin": "^0.8.0",
        "lint-staged": "^15.4.1",
        "prettier": "^3.4.2",
        "prettier-plugin-blade": "^2.1.19",
        "vite": "^4.0.0"
    },
    "lint-staged": {
        "*.*": [
            "php ./vendor/bin/pint",
            "prettier --write"
        ]
    }
}
